1. В примере с анимацией аккордион. 
а) Сейчас там два вложенный списка и они могут быть открыты одновременно.
Дописать код что б одновренменно мог быть открыт только один вложенный список.
Т.е. все(их может быть больше) списки закрываются, а потом нужный открывается.
б) Дописать анимацию, что б плавно раскрывался и сворачивался.

2. Написать калькулятор, загатовка в папке calc

3. В примере Drag'N'Drop применяется var self = this; 
а) Разобраться зачем он там.
б) В данный момент что б остановить движения шарика мы используем такой код:
document.onmousemove = function(e) {
    e = fixEvent(e);
    moveAt(e);
  }; 


Ниже закомментирован такой код:
document.addEventListener('mousemove', function(e){
    e = fixEvent(e);
    moveAt(e);
  });

Если их поменять местами (один закоментировать, другой наоборот), то шарик не прекратит свое движение за мышкой.
Задача:

Дописать код с использованием document.addEventListener. Либо же объяснить почему его тут использовать нельзя